buildscript {
    ext.kotlin_version = '1.1+'

    repositories {
        jcenter()
        maven {
            url 'https://maven.google.com/'
            name 'Google'
        }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.1'
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
    }
    lintOptions {
        abortOnError false
    }
}

repositories {
    jcenter()
    maven {
        url 'https://maven.google.com/'
        name 'Google'
    }
    maven { url "https://www.jitpack.io" }
    maven {
        url "https://api.bitbucket.org/2.0/repositories/smartpesa/maven/src/master"
        credentials {
            username "smartpesa-sdk-maven"
            password "tagGh44qHsBa39uwNurB"
        }
        authentication {
            basic(BasicAuthentication)
        }
    }
}

dependencies {
    implementation 'com.facebook.react:react-native:+'
    implementation 'com.smartpesa:sdk:3.2.8'
}